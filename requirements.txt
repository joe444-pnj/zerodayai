# ─────────────────────────────────────────
#  ZeroDay AI — Dependency Manifest
# ─────────────────────────────────────────

# Core CLI & Utilities
rich>=13.7.0
click>=8.1.7
pyyaml>=6.0.1
python-dotenv>=1.0.1
aiofiles>=23.2.1
tabulate>=0.9.0

# LLM Integration (Ollama local models)
ollama>=0.2.1

# Vector Store / RAG (lightweight CVE search)
chromadb>=0.5.3

# Static Analysis
bandit>=1.7.9
# NOTE: semgrep — install with: pip install semgrep
#       may need extra steps on Windows (WSL recommended for full support)

# Web / HTTP Fuzzing
requests>=2.31.0
httpx>=0.27.0
aiohttp>=3.9.5
beautifulsoup4>=4.12.3
lxml>=5.2.0

# Database (findings storage)
sqlalchemy>=2.0.30
aiosqlite>=0.20.0

# FastAPI backend (dashboard API)
fastapi>=0.111.0
uvicorn[standard]>=0.30.0
websockets>=12.0
sse-starlette>=1.8.2

# Headless Browser & Automation (SiteImager tool)
playwright>=1.40.0

# Network Scanning
# python-nmap>=0.7.1  # Uncomment on Linux (requires nmap binary)

# Reporting
jinja2>=3.1.4
markdown>=3.6.0

# Security / Crypto Helpers
cryptography>=42.0.0

# NOTES: Optional tools (install separately as needed)
# ffuf — cargo install ffuf (Go tool, fast fuzzer)
# nuclei — https://nuclei.projectdiscovery.io/ (vulnerability templates)
# sqlmap — pip install sqlmap (SQL injection testing)
# nikto — https://cirt.net/nikto2 (web scanner)
# dirsearch — pip install dirsearch (directory enumeration)
# semgrep — pip install semgrep (SAST)
